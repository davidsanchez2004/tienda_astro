---
import AdminLayout from '../../layouts/AdminLayout.astro';
import AdminDashboard from '../../components/admin/AdminDashboard';

// NOTA: La verificación de autenticación se hace del lado del cliente (React)
// porque el servidor no puede ver las cookies del navegador debido al proxy
console.log('[Dashboard SSR] Rendering - auth check done on client side');
---

<AdminLayout title="Admin - Dashboard">
  <AdminDashboard client:load />
</AdminLayout>
